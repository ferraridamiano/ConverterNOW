name: calculator_widget
description: Calculator widget for Converter NOW
publish_to: 'none'
resolution: workspace

version: 1.0.0+1

environment:
  sdk: ">=3.9.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  # Calculator computation
  decimal: ^3.2.4
  # State management
  flutter_riverpod: ^2.6.1
  # Calculator computation
  rational: ^2.2.3
  # App translation
  translations:
    path: ../translations

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
