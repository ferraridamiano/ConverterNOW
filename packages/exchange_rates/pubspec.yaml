name: exchange_rates
description: Exchange rates api from ECB
version: 1.0.0
publish_to: none

environment:
  sdk: '>=2.17.0 <3.0.0'

dependencies:
  # Needed for downloading exchange rates from internet
  http: ^0.13.4
  # Needed for DateFormat
  intl: ^0.17.0

dev_dependencies:
  lints: ^2.0.0
  test: ^1.16.0
